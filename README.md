simple discord bot
was gonna be made using twilight-rs but i actually cannot rust

dependencies(very ephemeral, will probably migrate to rust later):

```sh
pip install - requirements.txt
```

i suggest creating an isolated environment
```sh
python -m venv env
```
